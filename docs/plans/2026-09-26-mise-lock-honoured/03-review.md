# U3 — Review: the setup:mise and setup:all shell tasks

- **Wave:** 2
- **Depends on:** U1, U2
- **Owns:** —
- **Model:** opus
- **Kind:** review

## Scope

Covers U1 (the mise pack's `setup/mise`, `setup/all`, `mise.toml` comment) and
U2 (this repo's `setup/mise`, `setup/all`, `mise.toml` comment) — the branch
delta since the branch base, the first and only review row. Reason (decision 9):
the change ships shell scripts, which is runnable code.
