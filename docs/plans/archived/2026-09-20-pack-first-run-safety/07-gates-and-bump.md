# U7 — gates and bump: four pack bumps, stackgen 1.22.0, site 1.1.34

- **Wave:** 4
- **Depends on:** U6
- **Owns:** `plugins/stackgen/stacks/toolchain-manager/mise/pack.yaml`,
  `plugins/stackgen/stacks/toolchain-gate/pre-commit/pack.yaml`,
  `plugins/stackgen/stacks/toolchain-gate/gitleaks/pack.yaml`,
  `plugins/stackgen/stacks/toolchain-gate/grype/pack.yaml`,
  `plugins/stackgen/stacks/bundles/mise.md`,
  `plugins/stackgen/stacks/bundles/repo-gates.md`,
  `plugins/stackgen/stacks/inventory.md` (regenerated, never hand-edited),
  `plugins/stackgen/.claude-plugin/plugin.json`, `site/package.json`,
  `.claude-plugin/marketplace.json` (regenerated, never hand-edited)
- **Model:** opus
- **Kind:** edit
- **Read first:** every owned file, top to bottom, before editing; index.md's
  Consent block and decision 8.
- **Lazy-load:** `.claude/skills/plugin-authoring/references/structure.md:35-48`
  (the version rule).

## Ruling

Decision 8 — Pack bumps: "Every `pack.yaml` bump, its bundle pin and the
inventory regeneration sit in **U7** so they land in one commit; wave-1 units
change content only. mise `1.2.3` → `1.3.0`, pre-commit `1.1.1` → `1.1.2`,
gitleaks `1.1.0` → `1.1.1`, grype `1.0.0` → `1.0.1`; dprint untouched."

The Consent block, verbatim:

- "Release stackgen publicly — minor — `1.21.0` → `1.22.0`, a hand edit of
  `plugins/stackgen/.claude-plugin/plugin.json` then
  `mise run p:plugins:marketplace`; no release step"
- "Release vwf publicly — none — untouched"
- "Release site publicly — patch — `1.1.33` → `1.1.34` via
  `mise run p:site:version`; no release step"
- "Release installer publicly — none — untouched"

No target lands on a component equal to 13 or 17.

## Edits

1. **`site/package.json`** — run `mise run p:site:version` bare (patch is its
   default; it refuses a dirty tree, so run it first), reaching `1.1.34`.
2. The four **`pack.yaml`** version lines: mise `:6` `1.2.3` → `1.3.0`;
   pre-commit `:5` `1.1.1` → `1.1.2`; gitleaks `:4` `1.1.0` → `1.1.1`; grype
   `:4` `1.0.0` → `1.0.1`.
3. The pins: **`bundles/mise.md:7`** → `@1.3.0`; **`bundles/repo-gates.md`**
   `:10` pre-commit → `@1.1.2`, `:8` gitleaks → `@1.1.1`, `:9` grype → `@1.0.1`;
   `:7` dprint untouched.
4. Run `mise run p:plugins:inventory` so `inventory.md` carries the four new
   versions (rows `:98-103` and the bundle row `:152` today).
5. **`plugins/stackgen/.claude-plugin/plugin.json`** — `"version"` `1.21.0` →
   `1.22.0`.
6. Run `mise run p:plugins:marketplace` so `.claude-plugin/marketplace.json`
   pins `stackgen-v1.22.0`; vwf's pin is unchanged. The gitignored dev manifest
   is regenerated by the same run and never committed.

## Verification

- The full wave gate:

      mise run p:plugins:marketplace -- --check
      mise run p:plugins:inventory -- --check
      mise run p:plugins:check
      mise run p:plugins:shellcheck
      mise run p:plugins:npm-normalize-test
      mise run code:precommit
      mise run p:site:check

- `git diff --stat` shows exactly the ten owned files changed (plus the site
  lockfile only if `p:site:version` touches it — report it as `CHANGED:`).

## Guardrails

- No edit outside the owned files; the four bumps, the four pins and the
  inventory must all be in the tree when the orchestrator commits this unit.
- A version is plain `X.Y.Z` — no build metadata, no `v` prefix.
- `p:site:version` refuses a dirty tree — run it before anything else.
- Delete with `rm`, never `git rm`.

## Commit

`ops: stackgen 1.22.0, site 1.1.34 — pack first-run safety` — written by the
orchestrator after the wave gate. Type from
`.config/git-conventional-commits.yaml`; no scopes.
