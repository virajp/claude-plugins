# U5 — Review: the code:lint tasks and the mise pack payload

- **Wave:** 2
- **Depends on:** U1, U2, U3
- **Owns:** —
- **Model:** opus
- **Kind:** review

## Scope

Covers U1 (the five callers' `code:lint` tasks and `mise.d/`), U2 (markdown's
new `code:lint`) and U3 (init's mise pack payload and merge rule) — the branch
delta since the branch base, the first and only review row. Reason (decision 7):
the `code:lint` tasks change.
