# U9 — Review: the moved task library, the pack scripts and the checker

- **Wave:** 3
- **Depends on:** U3, U5, U8
- **Owns:** —
- **Model:** opus
- **Kind:** review

## Scope

Covers U3 (the five packs' scripts), U5 (`check.ts`, `check.test.ts`,
`inventory.ts`, the shellcheck task) and U8 (the task library's `code:graph` and
`setup:ai`), and U1 transitively (the move) — the branch delta since the branch
base, the first and only review row. U2, U4, U6, U7 are skill prose in the
range, not covered. Reason (decision 12): shell tasks, the checker and the task
library move.
