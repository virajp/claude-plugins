# U7 — Review: the checker, the inventory and the moved task library

- **Wave:** 3
- **Depends on:** U2, U6
- **Owns:** —
- **Model:** opus
- **Kind:** review

## Scope

Covers U2 (`scripts/src/check.ts`, `check.test.ts`, `inventory.ts`, the
shellcheck task) and U6 (the moved packs' payload, including shell task files),
and U1 transitively (the move itself) — the branch delta since the branch base,
the first and only review row. U3–U5 are skill prose in the range, not covered.
Reason (decision 9): `check.ts`, `inventory.ts` and shell tasks change.
