# U6 — Review: the pack scripts, init's mise pack and the checker

- **Wave:** 2
- **Depends on:** U1, U2, U5
- **Owns:** —
- **Model:** opus
- **Kind:** review

## Scope

Covers U1 (the five packs, including swiftui's `_scripts/xcode`,
`tasks/test/golden`, doppler's `tasks/setup/secrets`), U2 (init, including
`packs/mise/**` task files) and U5 (`check.ts`, `check.test.ts`) — the branch
delta since the branch base, the first and only review row. U3 and U4 are skill
prose in the range, not covered. Reason (decision 9): shell tasks and the
checker change.
