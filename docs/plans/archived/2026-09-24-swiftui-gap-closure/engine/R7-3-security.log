/security-review — R7 round 3 — range b14ada40..2091563e

No high-confidence vulnerabilities found in b14ada40..HEAD.

Candidates considered and dropped:
- _scripts/xcode require_pin_source: ${!name} indirect expansion and env.${name} take names only from fixed arguments in task/skill code (XCODE_VERSION, SIMULATOR_*); no untrusted input. Score 2.
- mise config get --file reading the fragment: path from MISE_PROJECT_ROOT (env, trusted); the value read is only string-compared, never executed. Score 2.
- ux-gate step 2 mise x -- bash -c sourcing the repo's helpers: runs repo-committed scripts, same trust as any task; mise config-trust prompt still applies. Score 2.
- scripts/src/check.ts new TOML header/key regexes: read-only, nothing spawned; ReDoS excluded. Score 1.
