/security-review — R7 round 1 — range b14ada40..233ef4ec

No high-confidence vulnerabilities found in b14ada40..HEAD.

- TOML injection via a detected value written into conf.d/swiftui.toml — dropped 3/10: the value comes from xcrun simctl / xcodebuild on the developer's own machine and is confirmed at setup; no untrusted party controls it.
- Command injection via SIMULATOR_* in test/golden and _scripts/xcode — dropped 2/10: the delta changes message text only; env/CLI inputs are trusted.
- machine_env.detect and binaries[].probe run through mise x -- sh -c — dropped 2/10: maintainer-authored in the shipped pack.yaml, trusted like code.
- Path traversal in scripts/src/check.ts (packFactFaults, tomlEnvKeys) — dropped 1/10: read-only, fixed subdirectory; lockfile entries refuse .. and absolute paths.
- Doctor running a maintainer-authored lockfile glob — dropped 2/10: presence check only.
