# U9 — Review: the checker, the gate assets and this repo's graphify hook

- **Wave:** 3
- **Depends on:** U5, U8, U12
- **Owns:** —
- **Model:** opus
- **Kind:** review

## Scope

Covers U5 (`check.ts`, `check.test.ts`, `inventory.ts`, the shellcheck task), U8
(the gate assets and the task library's `setup/precommit`) and U12 (this repo's
`code/graph` task and pre-commit hook), and U1 transitively (the move) — the
branch delta since the branch base, the first and only review row. U2, U3, U4,
U6, U7 are skill and pack prose or YAML in the range, not covered. Reason
(decision 10): the checker, this repo's task and hook, and the task library's
`setup/precommit`.
