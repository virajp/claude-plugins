/security-review — R7 round 2 — range b14ada40..eb3b78eb

No high-confidence vulnerabilities found in b14ada40..HEAD.

Dropped candidates:
- _scripts/xcode require_pin_source: grep/sed patterns take ${name} from fixed literal arguments the pack passes (XCODE_VERSION, SIMULATOR_*); file values reach only a string comparison and quoted print_* calls; no eval, no word-splitting. Score 1.
- materialize.md machine-env write, TOML injection: control characters refused, then a TOML basic string with " and \ escaped — closes string breakout and key injection. Score 2.
- Same value as a mise Tera template ({{ exec(...) }} in an [env] value): escaping does not neutralise it, but the value comes from the person answering or this machine's own xcodebuild/simctl output; no concrete untrusted source. Hardening note (refuse {{ / {%), not a finding. Score 3.
- detect run through mise x -- sh -c: maintainer-authored pack.yaml or the materialized payload; running setup already trusts the repo's mise config. Score 2.
- test/golden -destination built from pin and flag values: a single quoted argv item; env and CLI flags trusted. Score 1.
- check.ts packFactFaults: validation only, no sink. Score 1.
