# U6 — gates and bump: vwf 19.38.0, site 1.1.33

- **Wave:** 3
- **Depends on:** U5
- **Owns:** `plugins/vwf/.claude-plugin/plugin.json`, `site/package.json`,
  `.claude-plugin/marketplace.json` (regenerated, never hand-edited)
- **Model:** opus
- **Kind:** edit
- **Read first:** every owned file, top to bottom, before editing; index.md's
  Consent block.
- **Lazy-load:** `.claude/skills/plugin-authoring/references/structure.md:35-48`
  (the version rule).

## Ruling

The Consent block, verbatim:

- "Release vwf publicly — minor — `19.37.0` → `19.38.0`, a hand edit of
  `plugins/vwf/.claude-plugin/plugin.json` then
  `mise run p:plugins:marketplace`; no release step"
- "Release stackgen publicly — none — untouched"
- "Release site publicly — patch — `1.1.32` → `1.1.33` via
  `mise run p:site:version`; no release step"
- "Release installer publicly — none — untouched"

Neither target lands on a component equal to 13 or 17, so no skip applies.

## Edits

1. **`site/package.json`** — run `mise run p:site:version` bare (patch is its
   default; it takes no positional and refuses a dirty tree, so run it first),
   reaching `1.1.33`.
2. **`plugins/vwf/.claude-plugin/plugin.json`** — `"version"` `19.37.0` →
   `19.38.0`.
3. Run `mise run p:plugins:marketplace` so `.claude-plugin/marketplace.json`
   pins `vwf-v19.38.0`; stackgen's pin is unchanged. The gitignored dev manifest
   is regenerated by the same run; it is never committed.

## Verification

- The full wave gate:

      mise run p:plugins:marketplace -- --check
      mise run p:plugins:inventory -- --check
      mise run p:plugins:check
      mise run code:precommit
      mise run p:site:check

- `git diff --stat` shows exactly the three owned files changed (plus the site
  lockfile only if `p:site:version` touches it — report it as `CHANGED:` if so).

## Guardrails

- No edit outside the three owned files; `plugins/stackgen/**` is untouched by
  this plan and its version does not move.
- A version is plain `X.Y.Z` — no build metadata, no `v` prefix.
- `p:site:version` refuses a dirty tree — run it before anything else in this
  unit.
- Delete with `rm`, never `git rm`.

## Commit

`ops: vwf 19.38.0, site 1.1.33 — setup reshape triggers` — written by the
orchestrator after the wave gate. Type from
`.config/git-conventional-commits.yaml`; no scopes.
