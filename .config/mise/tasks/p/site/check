#!/usr/bin/env bash
#MISE description="Type-check, build and link-check the website (the site gate)"
#MISE dir="{{ config_root }}/site"

# The site gate: what the wave gate and `p:site:release` run.

set -e

pnpm exec astro check
mise run p:site:build
# Every internal href in dist/**/*.html must resolve to a built file, and every
# #fragment to an id in its target. Then the same over the markdown mirror:
# every absolute site URL in dist/**/*.md, llms.txt and llms-full.txt resolves,
# and every docs page carries exactly one markdown alternate link.
pnpm exec tsx scripts/check-links.ts
