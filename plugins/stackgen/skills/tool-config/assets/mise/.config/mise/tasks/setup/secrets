#!/usr/bin/env bash

#MISE description="Install and configure this repo's secret manager"
#MISE hide=true
#PLACEHOLDER

set -euo pipefail
# shellcheck source=/dev/null
source "${MISE_PROJECT_ROOT}/.config/mise/tasks/_scripts/placeholder"

# Every product needs a secret mechanism; which tool provides it is a choice.
# The slot is the contract — secrets reach a process as environment variables,
# never read from a committed plaintext file, resolved per environment, and
# authenticated non-interactively in CI. The pinned capability-provider
# component overwrites this file with the setup its own tool needs, and ships
# that tool's pin in `.config/mise/conf.d/<provider>.toml` rather than here.

placeholder_notice "setup:secrets" "no secret manager is pinned for this repo"
