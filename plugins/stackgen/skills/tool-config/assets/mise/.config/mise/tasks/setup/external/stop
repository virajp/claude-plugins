#!/usr/bin/env bash

#MISE description="Stop the external services this repo runs against locally"
#MISE hide=true
#PLACEHOLDER

set -euo pipefail
# shellcheck source=/dev/null
source "${MISE_PROJECT_ROOT}/.config/mise/tasks/_scripts/placeholder"

# Local-only, like the rest of `setup:external:*` — see `start` for why.
if [[ ",${MISE_ENV:-}," != *",dev,"* ]]; then
  exit 0
fi

placeholder_notice "setup:external:stop" "no external services are pinned for this repo"
