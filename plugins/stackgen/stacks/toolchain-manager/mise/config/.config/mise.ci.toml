# CI builds AND the deployed runtime — loaded when MISE_ENV=ci.
#
# One variant, two roles: the pipeline and production read the same layer, so
# the production overrides live here rather than in a fourth file nothing
# selects.

[settings]
# Install exactly what the lockfiles record and fail rather than resolve.
# `lockfile = true` in the base keeps them current on a laptop; this line is
# what makes the pipeline a reader of them. A CI run that silently resolves a
# newer version is a build nobody can reproduce afterwards.
#
# There is one lockfile per config file that declares tools, named after that
# file's stem — with the split as shipped, `mise.dev.lock` alone. All of them
# are tracked, so the pipeline reads the same versions a laptop resolved.
locked = true

# Per-runtime CI workarounds go here and nowhere else.
#
# Node: CI runs on Linux, where mise's bundled Node release-key gpg import can
# fail ("no valid OpenPGP data found"). Disable ONLY the Node signature check —
# the tarball is still SHA256-verified, and the general gpg_verify = true in
# mise.toml stays intact. Uncomment for a Node project:
#   node.gpg_verify = false

[tools]
# Usually empty — CI reuses the runtime from mise.toml. Add a tool here only if
# the pipeline genuinely needs it and development does not.

[env]
# Empty by default. What belongs here: CI-only and production VALUES for keys
# mise.dev.toml already names locally (`LOG_LEVEL`, `RUNTIME_ENV`, a deploy
# target). What never belongs here: a secret. Tokens and credentials are
# injected by the CI provider or resolved by the pinned secret manager at run
# time — a value committed to this file is a value in the repo's history.
