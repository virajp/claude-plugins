# The repo's ignore set. Sections are banners; keep them, and keep the
# why-comments — a pattern nobody can explain is a pattern nobody dares delete.

# ==== macOS ====
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# ==== Editors ====
# .idea/ is per-machine index and workspace state, regenerated on open.
# .vscode/ is deliberately NOT here: what a repo puts there — extensions.json,
# launch.json, tasks.json — is hand-written and shared, so whether to commit it
# is the repo's call, not this pack's.
.idea/
*.swp
*.swo

# ==== AI tooling ====
# One developer's agent state. The shared half — settings.json, skills/,
# agents/ — stays tracked, because it is repo configuration.
.claude/settings.local.json
.claude/worktrees/
.claude/cache/
.claude/todos.json
.worktrees/
.opencode/

# ==== graphify ====
# The code-intelligence graph is rebuilt from the tree on demand, so the graph
# itself is machine-local. GRAPH_REPORT.md is the one artifact worth diffing —
# it is prose about what the graph found, and a review can read it — so the
# negation follows the pattern that would otherwise swallow it.
graphify-out/*
!graphify-out/GRAPH_REPORT.md

# ==== mise ====
# The uncommitted local override, at every path mise loads one from.
mise.local.toml
.mise.local.toml
.config/mise.local.toml
.config/mise.*.local.toml
.config/mise/config.*.local.toml
mise.local.lock

# ==== Secrets and env ====
# A value never enters the tree. .env.example is the names-only template and
# IS tracked, so its negation has to stay after the .env.* pattern that would
# otherwise swallow it.
.env
.env.*
!.env.example
# The local-first secrets manager's machine-local override; the file beside it
# holds references and ciphertext only, and is committed.
fnox.local.toml
# Private keys and identities: the one hit that is always real.
*.pem
*.key
*.p12

# ==== Scratch ====
# Local-only working notes, never reviewed and never shipped.
docs/scratchpad/

# ==== Reports ====
# Gate output is regenerable, and a scanner report quotes what it found.
gitleaks-report.*
grype-report.*
sbom.*

# Stack sections are appended below this line by /vwf:init, one
# `# ==== <Name> ====` banner each, from github/gitignore.
